language: python

python:
    - "2.7"
    - "3.2"
    - "3.3"

install:
    - python setup.py --quiet install

script:
    - ./setup.py test
    - yolk --latest-releases=1
    - yolk --show-updates
    - yolk --query-metadata=yolk
